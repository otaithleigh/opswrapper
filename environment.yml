name: opswrapper
channels:
  - defaults
  - conda-forge
dependencies:
  - python >= 3.7
  - numpy
  - toml
  - xarray
  # Testing
  - ipython
  - line_profiler
  - yapf
  - pytest
  # Needed for examples
  - matplotlib
  # Documentation
  - sphinx
  - doc8
  # Development install
  - pip
  - pip:
    - "-e ."
